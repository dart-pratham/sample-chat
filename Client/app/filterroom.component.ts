import { Component } from '@angular/core';
import { Message } from './message';

@Component({
  //moduleId: module.id,
  selector: 'filter-room',
  template: '<h1>Filter Room</h1>'
})

export class FilterroomComponent { }
